defaults
  mode http
  timeout client 10s
  timeout connect 5s
  timeout server 10s
  timeout http-request 10s
  log global

listen stats
  bind *:15000
  balance
  mode http
  stats enable
  stats auth admin:admin

# frontend myfrontend
#     bind *:80
#     default_backend myservers

# backend myservers
#     server server1 microbot1
#     server server2 microbot2